{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Output Schema for lRange Operation",
  "description": "Output schema for the lRange operation in the connector.",
  "properties": {
    "payload": {
      "type": "object",
      "description": "The main response payload from the lRange operation."
    },
    "headers": {
      "type": "object",
      "description": "Transport headers returned by the lRange operation.",
      "additionalProperties": true
    },
    "attributes": {
      "type": "object",
      "description": "Metadata about the lRange operation.",
      "properties": {},
      "additionalProperties": false
    }
  },
  "required": [
    "payload",
    "headers",
    "attributes"
  ],
  "additionalProperties": false
}