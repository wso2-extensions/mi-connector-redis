{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Output Schema for connection.redis Operation",
  "description": "Output schema for the connection.redis operation in the connector.",
  "properties": {
    "payload": {
      "type": "object",
      "description": "The main response payload from the connection.redis operation."
    },
    "headers": {
      "type": "object",
      "description": "Transport headers returned by the connection.redis operation.",
      "additionalProperties": true
    },
    "attributes": {
      "type": "object",
      "description": "Metadata about the connection.redis operation.",
      "properties": {},
      "additionalProperties": false
    }
  },
  "required": [
    "payload",
    "headers",
    "attributes"
  ],
  "additionalProperties": false
}