{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Output Schema for expire Operation",
  "description": "Output schema for the expire operation in the connector.",
  "properties": {
    "payload": {
      "type": "object",
      "description": "The main response payload from the expire operation."
    },
    "headers": {
      "type": "object",
      "description": "Transport headers returned by the expire operation.",
      "additionalProperties": true
    },
    "attributes": {
      "type": "object",
      "description": "Metadata about the expire operation.",
      "properties": {},
      "additionalProperties": false
    }
  },
  "required": [
    "payload",
    "headers",
    "attributes"
  ],
  "additionalProperties": false
}