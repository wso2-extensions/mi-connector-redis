{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Output Schema for exists Operation",
  "description": "Output schema for the exists operation in the connector.",
  "properties": {
    "payload": {
      "type": "object",
      "description": "The main response payload from the exists operation."
    },
    "headers": {
      "type": "object",
      "description": "Transport headers returned by the exists operation.",
      "additionalProperties": true
    },
    "attributes": {
      "type": "object",
      "description": "Metadata about the exists operation.",
      "properties": {},
      "additionalProperties": false
    }
  },
  "required": [
    "payload",
    "headers",
    "attributes"
  ],
  "additionalProperties": false
}