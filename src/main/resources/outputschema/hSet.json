{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Output Schema for hSet Operation",
  "description": "Output schema for the hSet operation in the connector.",
  "properties": {
    "payload": {
      "type": "object",
      "description": "The main response payload from the hSet operation."
    },
    "headers": {
      "type": "object",
      "description": "Transport headers returned by the hSet operation.",
      "additionalProperties": true
    },
    "attributes": {
      "type": "object",
      "description": "Metadata about the hSet operation.",
      "properties": {},
      "additionalProperties": false
    }
  },
  "required": [
    "payload",
    "headers",
    "attributes"
  ],
  "additionalProperties": false
}