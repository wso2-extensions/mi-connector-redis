{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Output Schema for lInsert Operation",
  "description": "Output schema for the lInsert operation in the connector.",
  "properties": {
    "payload": {
      "type": "object",
      "description": "The main response payload from the lInsert operation."
    },
    "headers": {
      "type": "object",
      "description": "Transport headers returned by the lInsert operation.",
      "additionalProperties": true
    },
    "attributes": {
      "type": "object",
      "description": "Metadata about the lInsert operation.",
      "properties": {},
      "additionalProperties": false
    }
  },
  "required": [
    "payload",
    "headers",
    "attributes"
  ],
  "additionalProperties": false
}